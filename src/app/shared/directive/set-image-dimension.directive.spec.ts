import { SetImageDimensionDirective } from './set-image-dimension.directive';

describe('SetImageDimensionDirective', () => {
  it('should create an instance', () => {
    const directive = new SetImageDimensionDirective();
    expect(directive).toBeTruthy();
  });
});
